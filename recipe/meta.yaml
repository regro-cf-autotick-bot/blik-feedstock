{% set name = "blik" %}
{% set version = "0.6.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/blik-{{ version }}.tar.gz
  sha256: 7aef27f9dda7db1e137f5a38b7401248146e77940dbbbe27e46ed9da14ce325a

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - hatch-vcs
    - hatchling
    - pip
    - python >=3.9
  run:
    - cryohub >=0.5.1
    - cryotypes >=0.2.0
    - dask-core
    - einops
    - magicgui >=0.4.0
    - morphosamplers >=0.0.5
    - napari
    - numpy
    - pandas
    - python >=3.9
    - scikit-image  # Not listed as requirement
    - scipy

test:
  imports:
    - blik
    - blik.reader
    - blik.utils
    - blik.writer
    - blik.widgets.file_reader
    - blik.widgets.filter
  commands:
    - pip list
    - pip check
  requires:
    - pip

about:
  home: https://pypi.org/project/blik
  summary: Python tool for visualising and interacting with cryo-ET and subtomogram averaging data.
  license: GPL-3.0-only
  license_file: LICENSE
  dev_url: https://github.com/gutsche-lab/blik

extra:
  recipe-maintainers:
    - goanpeca
    - jaimergp
